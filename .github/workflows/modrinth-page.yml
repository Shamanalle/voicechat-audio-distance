name: Modrinth page

# Uploads docs/modrinth.md as the Modrinth project page and the English summary from
# docs/store-description.md as its short description, so the page is edited with a commit.
# Needs the MODRINTH_TOKEN secret with the "Write projects" scope; without it the upload is skipped
# with a warning. Pull requests only check that the page and the summary can be read.

on:
  push:
    branches: [ "main" ]
    paths: [ "docs/modrinth.md", "docs/store-description.md", ".github/workflows/modrinth-page.yml" ]
  pull_request:
    paths: [ "docs/modrinth.md", "docs/store-description.md", ".github/workflows/modrinth-page.yml" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  page:
    name: Check and upload
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build the request
        run: |
          summary=$(grep -m1 '^\*\*Summary:\*\*' docs/store-description.md | sed 's/^\*\*Summary:\*\* *//')
          if [ -z "${summary}" ]; then
            echo "::error file=docs/store-description.md::No **Summary:** line"
            exit 1
          fi
          # Modrinth allows 3 - 256 characters in the summary
          if [ "${#summary}" -gt 256 ]; then
            echo "::error file=docs/store-description.md::The summary is ${#summary} characters; Modrinth allows 256"
            exit 1
          fi
          if [ ! -s docs/modrinth.md ]; then
            echo "::error file=docs/modrinth.md::The page is empty"
            exit 1
          fi
          jq -n --arg description "${summary}" --rawfile body docs/modrinth.md \
            '{description: $description, body: $body}' > request.json
          echo "Summary: ${#summary} characters; page: $(wc -c < docs/modrinth.md) bytes"

      - name: Upload to Modrinth
        if: github.event_name != 'pull_request'
        env:
          MODRINTH_TOKEN: ${{ secrets.MODRINTH_TOKEN }}
        run: |
          project=$(jq -r '.modrinth // ""' .github/publish.json)
          if [ -z "${project}" ] || [ -z "${MODRINTH_TOKEN}" ]; then
            echo "::warning::Modrinth page skipped: set \"modrinth\" in .github/publish.json and the MODRINTH_TOKEN secret"
            exit 0
          fi
          status=$(curl -sS -o response.txt -w '%{http_code}' -X PATCH \
            -H "Authorization: ${MODRINTH_TOKEN}" \
            -H "Content-Type: application/json" \
            -H "User-Agent: Shamanalle/voice-physics (GitHub Actions)" \
            --data @request.json \
            "https://api.modrinth.com/v2/project/${project}")
          if [ "${status}" = "204" ]; then
            echo "Modrinth page updated"
          elif [ "${status}" = "401" ] || [ "${status}" = "403" ]; then
            echo "::error::Modrinth refused the token (${status}): give MODRINTH_TOKEN the \"Write projects\" scope"
            cat response.txt
            exit 1
          else
            echo "::error::Modrinth answered ${status}"
            cat response.txt
            exit 1
          fi
